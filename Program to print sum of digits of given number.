#include<iostream>
using namespace std;

int main()
{
    int num,sum = 0;
    cout<<"Enter one number: ";
    cin>>num;
    while(num!=0)
    {
        sum += num%10;
        num = num/10;
    }
    cout<<"Sum of the digits of a number: "<<sum;
    return 0;
}
